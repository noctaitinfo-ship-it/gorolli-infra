# GoRolli — SEIS 2026-07-06 (maksed & riigid)

## Kuhu jõudsime

**Poliitika: KÕIK AVATUD (default allow), charge ALATI EUR.**

- Iga riik maailmas (ka country_config'ita) saab broneerida ja maksta. Ainus blokk = admini käsitsi keeld (`country_status='DISABLED'` või lüliti false).
- 51 riiki country_config's: `live`/`ACTIVE`, kõik lülitid true, automaatne payout. Kehtiv seemnefail: `sql/country_config_open_all.sql` (VIIMANE — asendab default-deny ja 21/23/7 mudelid).
- Charge/ledger alati EUR — serveri guardid: `payments-checkout` (summa AINULT booking-realt, klient saadab vaid booking_id), `payments-authorize` EUR-guard, Firebase `initPayment` FAAS0 guard. Kliendi saadetud currencyt ei usaldata kunagi (AUD-juhtum ei saa korduda).

**Stripe Checkout + Adaptive Pricing (uus maksevoog):**
- Klient näeb ja maksab OMA valuutas (NOK/JPY/PHP…), settlement + ledger EUR.
- `payments-checkout` loob sessiooni (base eur, adaptive_pricing on, capture automaatne — Adaptive ei toeta manual capture'i; makse toimub pickup-hetkel täies mahus).
- `payments-webhook` (deploy `--no-verify-jwt`) kinnitab: `payment_status='paid'`, `status='paid_confirmed'`, seob booking_id metadata + session-id kahesuunaliselt.
- `payments-authorize` (salvestatud kaart, EUR) jäi fallbackiks.
- Payout: `release-payout` automaatne; `connect-onboard` avatud; 7 riiki (CI GH IN ID KE NG ZA) lükkab Stripe ise Connectist tagasi → neile käsitsi (raha seisab GoRolli saldol, ledger peab arvet).

**Client app (flutterflow haru, pushitud):** avalik `/find` (lang/city/lat/lng/trailer_id/open=booking, auth-return), `_ribCheckout` pickup-voos (avab Checkout-URL-i, pollib paid-staatust, fallback vanale voole), 2 uut tõlkevõtit ×32 keelt, FAAS0 guard firebase/functions.

**Veeb (gorolli-web, haru redesign/gorolli-finder-demand-first):** Finder-avaleht, aktiivne client-kaardi embed auto-open kõigil seadmetel, 32 keelt, CTA-d → client.gorolli.com/find?lang=xx.

## SQL-failide järjekord (ajalugu; viimane võidab)
`country_config.sql` → `country_config_46_markets.sql` → `country_config_global_eur_live.sql` → `checkout_migration.sql` → **`country_config_open_all.sql`** (kehtiv poliitika)

## TEGEMATA / kontrolli
1. Supabase: `country_config_open_all.sql` käivitatud? + `supabase functions deploy release-payout connect-onboard` (peale viimast commiti `cb8161e`)
2. `firebase deploy --only functions` (FAAS0 guard live)
3. Stripe Dashboard: **Adaptive Pricing ENABLE**, webhook endpoint (`checkout.session.completed` + async_succeeded/failed), Supabase secret `STRIPE_WEBHOOK_SECRET`
4. Parent-repo (C:\Projects\GoRolli) remote on placeholder (`SINU_KASUTAJA`) — ~7 pushimata commiti; sea õige URL ja pushi
5. gorolli-web haru push (Cloudflare deploy), kui tegemata
6. Päris testmakse: pickup → Checkout avaneb → maksa → Supabase logis `[WEBHOOK] booking X PAID` → bookingul `payment_status='paid'`; kohalik valuuta nähtav välismaise IP/kaardiga

## Võtme-commitid
- Parent main: `a17ac8e` default-deny → `1bae613` 42P16 fix → `f04d56d` global EUR live → `0f7b6441` Checkout+webhook → `cb8161e` **OPEN ALL (kehtiv)**
- Client: `9f02bbc9` checkout-voog (kasutaja commitis flutterflow'le ise), `872e71da` FAAS0
